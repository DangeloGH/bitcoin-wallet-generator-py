## 📋 Requisitos e Instalação

Para executar este programa, você precisará ter o Python, algumas ferramentas de sistema e bibliotecas adicionais instaladas em seu computador. Siga os passos abaixo.

### Pré-requisitos de Sistema

1.  **Python 3:** É necessário ter o Python (versão 3.6 ou mais recente) instalado. O Python já vem com o gerenciador de pacotes `pip`, que usaremos em seguida.
    * Se você não tem o Python, baixe-o gratuitamente no site oficial: [python.org](https://www.python.org/)

2.  **Ferramentas de Compilação (Especialmente para Windows):**
    * **Para Windows:** Algumas das bibliotecas de criptografia (`bip_utils`) precisam ser compiladas (construídas) durante a instalação. Para que isso funcione, você precisa ter as Ferramentas de Build do C++ da Microsoft.
        * Baixe o instalador em: [Visual Studio Build Tools](https://visualstudio.microsoft.com/visual-cpp-build-tools/)
        * Ao executar o instalador, na aba "Cargas de Trabalho", certifique-se de marcar a opção **"Desenvolvimento para desktop com C++"**.
    * **Para macOS:** Geralmente, basta ter as ferramentas de linha de comando do XCode. Abra o Terminal e execute: `xcode-select --install`.
    * **Para Linux (Debian/Ubuntu):** Geralmente, basta ter o pacote `build-essential`. Abra o terminal e execute: `sudo apt-get install build-essential python3-dev`.

### Passos para Instalação

1.  **Obtenha o Código-Fonte:**
    * Vá até a página principal deste repositório no GitHub.
    * Clique no botão verde **"< > Code"**.
    * Selecione **"Download ZIP"** e salve o arquivo no seu computador.
    * Extraia o conteúdo do arquivo ZIP em uma pasta de sua preferência.

2.  **Abra um Terminal:**
    * **Windows:** Pressione a tecla `Windows`, digite `cmd` ou `PowerShell` e pressione Enter.
    * **macOS/Linux:** Abra o aplicativo "Terminal".

3.  **Navegue até a Pasta do Projeto:**
    * Use o comando `cd` (change directory) para entrar na pasta que você acabou de extrair.
    * *Exemplo:* Se você extraiu na sua Área de Trabalho, o comando seria: `cd Desktop/nome-da-pasta-do-projeto`

4.  **Instale as Bibliotecas Python:**
    * Com o terminal aberto na pasta correta, execute o comando abaixo para instalar as bibliotecas que o script precisa para funcionar.

    ```bash
    pip install mnemonic bip_utils "qrcode[pil]"
    ```

Pronto! Com as ferramentas de sistema e as bibliotecas Python instaladas, o ambiente está completamente configurado.
